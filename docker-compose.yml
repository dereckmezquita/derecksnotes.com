version: '3.8'

services:
  linode_derecksnotes_com_server:
    image: ${IMAGE_NAME}
    container_name: ${CONTAINER_NAME}
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - 3001:3001
    networks:
      - dereck-network
    restart: unless-stopped

networks:
  dereck-network:
    name: dereck-network
