/**
 * Placeholder hook — page view tracking is being rebuilt
 * as automatic read history (scroll-based).
 */
export function usePageView(_options: {
    slug: string;
    title: string;
    enabled?: boolean;
}) {
    // No-op placeholder
}
