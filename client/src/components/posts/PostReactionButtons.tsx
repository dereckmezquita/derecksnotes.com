'use client';

interface PostReactionButtonsProps {
    slug: string;
    title: string;
}

export function PostReactionButtons({ slug, title }: PostReactionButtonsProps) {
    return null; // Placeholder — will be rebuilt
}
