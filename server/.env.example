# =============================================================================
# Server Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values
#
# ENVIRONMENTS:
#   local - Running on your local machine (bare metal, no Docker)
#   dev   - Running on VPS in Docker (dev.derecksnotes.com)
#   prod  - Running on VPS in Docker (derecksnotes.com)
# =============================================================================

# -----------------------------------------------------------------------------
# BUILD ENVIRONMENT
# -----------------------------------------------------------------------------
# Values: 'prod' | 'dev' | 'local'
# Controls: cookie security, URL construction, CORS origins
BUILD_ENV=local

# -----------------------------------------------------------------------------
# SESSION / AUTHENTICATION
# -----------------------------------------------------------------------------
# Secret key for signing JWT tokens
# Generate with: openssl rand -base64 32
# REQUIRED in dev/prod, optional in local (uses placeholder)
SESSION_SECRET=replace_with_a_secure_random_string

# -----------------------------------------------------------------------------
# DATABASE
# -----------------------------------------------------------------------------
# SQLite database file path
# LOCAL: ./data/database.sqlite (relative to server directory)
# DEV/PROD: /app/data/database.sqlite (Docker volume mount)
# DATABASE_PATH=./data/database.sqlite

# -----------------------------------------------------------------------------
# OPTIONAL OVERRIDES
# -----------------------------------------------------------------------------
# These are auto-configured based on BUILD_ENV but can be overridden

# Server port (default: 3001)
# PORT=3001

# Domain for cookies (default: localhost / dev.derecksnotes.com / derecksnotes.com)
# DOMAIN=localhost

# Base URL for email links (default: http://localhost:3000 / https://...)
# BASE_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# ADMIN SEEDING
# -----------------------------------------------------------------------------
# Used when running: bun run db:seed
# Only needed once to create the initial admin user
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_secure_admin_password

# -----------------------------------------------------------------------------
# EMAIL (FUTURE)
# -----------------------------------------------------------------------------
# Email provider configuration - uncomment when ready to enable
# Supported: sendgrid, resend, ses (implement provider in services/email/)
#
# EMAIL_PROVIDER=sendgrid
# SENDGRID_API_KEY=your_sendgrid_api_key
#
# EMAIL_PROVIDER=resend
# RESEND_API_KEY=your_resend_api_key
